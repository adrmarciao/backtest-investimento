# Tasks: Migração para Material Design 3

## Setup & Theme
- [x] 1. Instalar dependências MUI no projeto frontend (`@mui/material`, `@emotion/react`, `@emotion/styled`, `@mui/icons-material`).
- [x] 2. Criar módulo de tema `src/theme/theme.js` configurando cores, tipografia e formato M3.
- [x] 3. Configurar `ThemeProvider` e `CssBaseline` em `src/main.jsx` ou `src/App.jsx`.

## Layout & Components Refactoring
- [x] 4. Refatorar o componente de Layout principal e Sidebar usando MUI `Drawer`, `AppBar`, `Toolbar` e `List`.
- [x] 5. Refatorar os cartões de resumo e métricas de backtest utilizando MUI `Card`, `CardContent` e `Typography`.
- [x] 6. Refatorar os formulários de entrada de parâmetros de investimento usando MUI `TextField`, `Select` e `Button`.
- [x] 7. Refatorar exibições de dados/tabelas para utilizar MUI `Table`, `TableBody`, `TableCell`, `TableContainer`.

## Verification
- [x] 8. Testar build do frontend (`npm run build`) e validar renderização visual no navegador.
